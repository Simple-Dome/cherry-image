services:
  app:
    image: infinite-canvas:local
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_BASE: ${VITE_BASE:-/}
    ports:
      - "3000:3000"
    restart: unless-stopped
