services:
  app:
    image: infinite-canvas:local
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_BASE: ${VITE_BASE:-/}
        VITE_FIXED_API_BASE_URL: ${VITE_FIXED_API_BASE_URL:-}
    ports:
      - "3000:3000"
    restart: unless-stopped
